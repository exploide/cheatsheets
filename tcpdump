# Piping tcpdump over SSH into Wireshark
ssh HOST "tcpdump -i any -U -w - 'not tcp port 22'" | wireshark -k -i -

# Filter expressions
## source or destination address
host 192.0.2.1
## source or destination port
tcp port 443
udp port 53
## logical operators
not arp
